<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el botón está activo -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/flight_light_blue"/>
            <!-- Radios redondeados en todas las esquinas para mantener la uniformidad -->
            <corners android:radius="20dp"/>
        </shape>
    </item>
    <!-- Estado cuando el botón está inactivo -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/flight_light_gray"/>
            <!-- Radios redondeados en todas las esquinas para mantener la uniformidad -->
            <corners android:radius="20dp"/>
        </shape>
    </item>
</selector>
